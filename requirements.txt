# Everything: the core plus every optional layer. This is what the Docker image installs.
-r requirements-core.txt

# HTTP client — ntfy, Navidrome, MusicBrainz/Deezer/lrclib lookups, install ping
requests>=2.31.0
# Web UI + JSON API (ENABLE_UI)
flask>=3.0
waitress>=3.0
# Fuzzy track matching — grabber, Navidrome playlist/star/play-count sync, suggestions
rapidfuzz>=3.9.0
# Cover-art handling — grabber tagging, playlist covers
pillow>=10.0.0,<11
# Download providers — grabber (GRABBER_ENABLED)
yt-dlp>=2024.8.0
streamrip>=2.1.0
