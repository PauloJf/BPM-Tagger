__pycache__/
*.py[cod]
*.egg-info/
.env
# Local compose overrides (reads .env; see docker-compose.yml for the template)
docker-compose.override.yml

# Runtime-generated files that may contain secrets or large local data
*.db
*.db-shm
*.db-wal
data/
*.csv

# Internal build plan — kept locally, not shipped in the public repo
PLAN.md

# Demo / dev helpers (not part of the shipped codebase)
demo_music/
seed_demo.py
inject_data.py
fix_paths.py
fix_demo_db.py
take_screenshots.py
screenshot_server.py
test_screen.png
test_window.png

# Claude Code
.claude/

# Local dev virtualenv & test caches
.venv/
.pytest_cache/
sample_music/
