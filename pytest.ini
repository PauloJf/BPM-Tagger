[pytest]
# Ensure the repo root is on sys.path so `import bpm_tagger` works regardless of
# how pytest is invoked (the `pytest` console script does not prepend CWD the way
# `python -m pytest` does — which is why CI failed to collect).
pythonpath = .
testpaths = tests
